@using TravelTripProject.Models.Classes
@model List<Blog>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<style>
    .table {
        width: 100%;
        border-collapse: collapse;
    }

        .table th,
        .table td {
            padding: 8px;
            text-align: left;
            vertical-align: middle;
        }

        .table thead th {
            background-color: #f8f9fa;
            color: #333;
            font-weight: bold;
        }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f8f9fa;
    }

    .table-hover tbody tr:hover {
        background-color: #f1f3f5;
    }
</style>

<h2>Blog Listesi</h2>
<br />
<table class="table table-bordered table-striped table-hover">
    <tr>
        <th>ID</th>
        <th>BAŞLIK</th>
        <th>SİL</th>
        <th>GÜNCELLE</th>
        <th>DETAYLAR</th>
    </tr>
    @foreach (var x in Model)
    {
        <tr>
            <td>@x.Id</td>
            <td>@x.Title</td>
            <td><a href="/Admin/DeleteBlog/@x.Id" class="btn btn-danger">Sil</a></td>
            <td><a href="/Admin/TakeBlog/@x.Id" class="btn btn-warning">Güncelle</a></td>
            <td><a href="/Blog/BlogDetail/@x.Id" class="btn btn-secondary">Detaylar</a></td>
        </tr>
    }
</table>
<a href="/Admin/NewBlog/" class="btn btn-primary">Yeni Blog</a>

